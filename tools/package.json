{
  "name": "dysizz-vie-tools",
  "private": true,
  "description": "Outils de build et de test (jamais installés sur le serveur Saltcorn)",
  "type": "module",
  "scripts": { "build": "node build.mjs", "test": "node ../tests/run.cjs" },
  "devDependencies": { "esbuild": "^0.25.0" }
}
